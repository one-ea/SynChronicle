import { z } from "zod";
const strings = z.array(z.string());
export const ChapterContractSchema = z.object({ required_beats: strings.optional(), forbidden_moves: strings.optional(), continuity_checks: strings.optional(), evaluation_focus: strings.optional(), emotion_target: z.string().optional(), payoff_points: strings.optional(), hook_goal: z.string().optional() }).strict(); export type ChapterContract = z.infer<typeof ChapterContractSchema>;
export const ChapterPlanSchema = z.object({ chapter: z.number().int().positive(), title: z.string(), goal: z.string(), conflict: z.string(), hook: z.string(), emotion_arc: z.string().optional(), notes: z.string().optional(), contract: ChapterContractSchema.optional() }).strict(); export type ChapterPlan = z.infer<typeof ChapterPlanSchema>;
export const ChapterSummarySchema = z.object({ chapter: z.number().int().positive(), summary: z.string(), characters: strings, key_events: strings }).strict(); export type ChapterSummary = z.infer<typeof ChapterSummarySchema>;
export const ArcSummarySchema = z.object({ volume: z.number().int().positive(), arc: z.number().int().positive(), title: z.string(), summary: z.string(), key_events: strings }).strict(); export type ArcSummary = z.infer<typeof ArcSummarySchema>;
export const VolumeSummarySchema = z.object({ volume: z.number().int().positive(), title: z.string(), summary: z.string(), key_events: strings }).strict(); export type VolumeSummary = z.infer<typeof VolumeSummarySchema>;
export const CharacterSnapshotSchema = z.object({ volume: z.number().int().nonnegative(), arc: z.number().int().nonnegative(), name: z.string(), status: z.string(), power: z.string().optional(), motivation: z.string(), relations: z.string().optional() }).strict(); export type CharacterSnapshot = z.infer<typeof CharacterSnapshotSchema>;
export const OutlineFeedbackSchema = z.object({ deviation: z.string(), suggestion: z.string() }).strict(); export type OutlineFeedback = z.infer<typeof OutlineFeedbackSchema>;
export const CharacterVoiceSchema = z.object({ name: z.string(), rules: strings }).strict(); export type CharacterVoice = z.infer<typeof CharacterVoiceSchema>;
export const WritingStyleRulesSchema = z.object({ volume: z.number().int().nonnegative(), arc: z.number().int().nonnegative(), prose: strings, dialogue: z.array(CharacterVoiceSchema), taboos: strings, updated_at: z.string() }).strict(); export type WritingStyleRules = z.infer<typeof WritingStyleRulesSchema>;
export const RelatedChapterSchema = z.object({ chapter: z.number().int().positive(), reason: z.string() }).strict(); export type RelatedChapter = z.infer<typeof RelatedChapterSchema>;
export const RecallItemSchema = z.object({ kind: z.string(), key: z.string().optional(), chapter: z.number().int().optional(), reason: z.string().optional(), summary: z.string().optional() }).strict(); export type RecallItem = z.infer<typeof RecallItemSchema>;
export const CommitResultSchema = z.object({ chapter: z.number().int().positive(), committed: z.boolean(), word_count: z.number().int().nonnegative(), next_chapter: z.number().int(), review_required: z.boolean(), review_reason: z.string().optional(), hook_type: z.string().optional(), dominant_strand: z.string().optional(), feedback: OutlineFeedbackSchema.nullable().optional(), arc_end: z.boolean().optional(), volume_end: z.boolean().optional(), volume: z.number().int().optional(), arc: z.number().int().optional(), needs_expansion: z.boolean().optional(), needs_new_volume: z.boolean().optional(), next_volume: z.number().int().optional(), next_arc: z.number().int().optional(), book_complete: z.boolean().optional(), flow: z.string().optional() }).strict(); export type CommitResult = z.infer<typeof CommitResultSchema>;
