import { createHash } from "node:crypto";
import { readFileSync } from "node:fs";
import { join } from "node:path";
export function checkBrandContract(root: string, packFiles: string[]): string[] { const issues: string[] = []; const pkg = JSON.parse(readFileSync(join(root, "package.json"), "utf8")) as { name?: string; bin?: Record<string, string>; license?: string }; if (pkg.name !== "synchronicle" || pkg.bin?.synchronicle !== "./dist/cli/index.js") issues.push("package identity"); if (pkg.license !== "Apache-2.0") issues.push("package license"); const license = readFileSync(join(root, "LICENSE")); if (createHash("sha256").update(license).digest("hex") !== "c71d239df91726fc519c6eb72d318ec65820627232b2f796219e87dcf35d0ab4") issues.push("LICENSE"); if (readFileSync(join(root, "NOTICE"), "utf8") !== "SynChronicle\nCopyright 2026 one-ea\n\nLicensed under the Apache License, Version 2.0.\n") issues.push("NOTICE"); for (const file of ["package/dist/cli/index.js", "package/README.md", "package/LICENSE", "package/NOTICE"]) if (!packFiles.includes(file)) issues.push(`pack:${file}`); return issues; }
