export class ReminderScheduler { private timers = new Set<NodeJS.Timeout>(); schedule(delayMs: number, callback: () => void): () => void { const timer = setTimeout(() => { this.timers.delete(timer); callback(); }, delayMs); this.timers.add(timer); return () => { clearTimeout(timer); this.timers.delete(timer); }; } close(): void { for (const timer of this.timers) clearTimeout(timer); this.timers.clear(); } }
